import './SubmitButton'
import { Button } from '@mui/material'

const SubmitButton = () => {
    return (
        <Button
            type='submit'
            className='submit-button'>
            Submit
        </Button>
    )
}

export default SubmitButton